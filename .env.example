# TeleForward - Telegram to Discord Message Forwarder
# 
# Usage:
#   1. Copy .env.example to .env and fill in your credentials
#   2. pip install -r requirements.txt
#   3. python main.py tui   (interactive setup)
#   4. python main.py run   (headless, for VPS)
#
# Environment variables:
#   TELEGRAM_API_ID     - Get from https://my.telegram.org
#   TELEGRAM_API_HASH   - Get from https://my.telegram.org
#   TELEGRAM_SESSION_STRING - (optional) Saved session for persistent login
#   DATABASE_PATH       - (optional) Path to SQLite database
#   DATA_DIR            - (optional) Directory for session/media storage (defaults to DATABASE_PATH parent)
#   LOG_LEVEL           - (optional) Logging level
#   DISCORD_ALLOW_MASS_MENTIONS - (optional) Allow @everyone/@here pings (default: false)
#   DISCORD_SUPPRESS_URL_EMBEDS - (optional) Wrap URLs in <> to prevent Discord link preview embeds (default: true)
#   DISCORD_STRIP_URLS - (optional) Remove URLs from forwarded text (default: false)
#   DISCORD_INCLUDE_TELEGRAM_LINK - (optional) Link embed title to t.me message (default: true)
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_SESSION_STRING=
DATABASE_PATH=data/teleforward.db
DATA_DIR=
LOG_LEVEL=INFO
DISCORD_ALLOW_MASS_MENTIONS=false
DISCORD_SUPPRESS_URL_EMBEDS=true
DISCORD_STRIP_URLS=false
DISCORD_INCLUDE_TELEGRAM_LINK=true
